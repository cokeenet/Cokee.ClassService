<UserControl x:Class="Cokee.ClassService.Views.Controls.StickyNote"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Cokee.ClassService.Views.Controls"
             mc:Ignorable="d" DataContext="{Binding Name}" DataContextChanged="UserControl_DataContextChanged">
    <Border VerticalAlignment="Stretch">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <TextBlock x:Name="name" Text="{Binding Name,StringFormat=From {0}}" d:Text="111" FontSize="18"/>
            <InkCanvas x:Name="ink" EditingMode="None" Background="Black" Grid.Row="1" Width="470" Height="300">
                <InkCanvas.DefaultDrawingAttributes>
                    <DrawingAttributes Color="White" Width="4.00314960629921" Height="4.00314960629921"/>
                </InkCanvas.DefaultDrawingAttributes>
            </InkCanvas>
        </Grid>
    </Border>

</UserControl>
