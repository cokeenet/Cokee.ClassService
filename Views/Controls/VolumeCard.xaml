<UserControl
    Height="140"
    Width="290"
    mc:Ignorable="d"
    x:Class="Cokee.ClassService.Views.Controls.VolumeCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Cokee.ClassService.Views.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border x:Name="audioCard" Background="{ui:AcrylicBrush}" BorderBrush="#FFE0BFBF" BorderThickness="2,2,2,2" CornerRadius="8">
        <Grid Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <ui:FontIcon Icon="{x:Static ui:FluentSystemIcons.Speaker2_24_Regular}" Margin="5" HorizontalAlignment="Center" VerticalAlignment="Center" />
            <ComboBox
                Grid.Column="1"
                Margin="5"
                MaxWidth="220"
                SelectionChanged="spk_SelectionChanged"
                VerticalAlignment="Center"
                x:Name="spk">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding FriendlyName}" />
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <TextBlock
                Grid.Row="1"
                Margin="5"
                MaxWidth="40"
                Text="--%"
                x:Name="vol" HorizontalAlignment="Center" VerticalAlignment="Center" />
            <Slider
                Grid.Column="1"
                Grid.Row="1"
                Margin="5"
                Maximum="100"
                MinWidth="200"
                ValueChanged="SliderChanged"
                x:Name="slider" VerticalAlignment="Center" />
            <Button
                Click="Button_Click"
                Grid.Column="1"
                Grid.Row="2"
                HorizontalAlignment="Right"
                Margin="5">

                <ui:FontIcon Icon="{x:Static ui:FluentSystemIcons.Dismiss_48_Regular}" />
            </Button>
        </Grid>
    </Border>
</UserControl>