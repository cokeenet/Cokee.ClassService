<UserControl
    x:Class="Cokee.ClassService.Views.Controls.StickyNote"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Cokee.ClassService.Views.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
    DataContext="{Binding Name}"
    DataContextChanged="UserControl_DataContextChanged"
    mc:Ignorable="d">
    <Border VerticalAlignment="Stretch" Style="{StaticResource CardBorderStyle}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition />
            </Grid.RowDefinitions>
            <TextBlock
                x:Name="name"
                d:Text="111"
                FontSize="18"
                Text="{Binding Name, StringFormat=From {0}}" />
            <InkCanvas
                x:Name="ink"
                Grid.Row="1"
                Width="470"
                Height="300"
                Background="Black"
                EditingMode="None">
                <InkCanvas.DefaultDrawingAttributes>
                    <DrawingAttributes
                        Width="4.00314960629921"
                        Height="4.00314960629921"
                        Color="White" />
                </InkCanvas.DefaultDrawingAttributes>
            </InkCanvas>
        </Grid>
    </Border>
</UserControl>